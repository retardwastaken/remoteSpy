local R2S = Instance.new("ScreenGui")
local Main = Instance.new("Frame")

R2S.Name = "R2S"
R2S.Parent = game.CoreGui
R2S.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Main.Name = "Main"
Main.Parent = R2S
Main.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Main.BorderSizePixel = 0
Main.Size = UDim2.new(1, 0, 1, 0)

return R2S
